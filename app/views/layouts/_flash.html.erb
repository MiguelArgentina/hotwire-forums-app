<% flash.each do |type, msg| %>
  <div class="alert alert-<%="#{alert_class(type)}"%> alert-dismissible fade show my-4" role="alert">
    <%= msg %>
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
  </div>
<% end %>
